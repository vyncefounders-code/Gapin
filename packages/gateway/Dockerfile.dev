FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
COPY .npmrc ./

# Install dependencies
RUN npm install

# Copy source
COPY . .

EXPOSE 3000

# Start in dev mode (nodemon)
CMD ["npm", "run", "dev"]
